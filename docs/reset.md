# 🔄 Spotify 이탈 예측 프로젝트 - 작업 히스토리 및 재시작 가이드

**작성일**: 2025-11-20  
**담당자**: 3번 (Preprocessing Validation + Feature Tuning)  
**상태**: 데이터 가공 완료, 내일 EDA부터 재시작 예정

#### 노트북 타임라인 정리

- **① `preprocessing_validation.ipynb`**  
  - 초기 전처리/기초 통계 수집 + 간단한 FE 생성/검증 (raw 데이터 기준)
- **② `FE_validation.ipynb`**  
  - 여러 FE 세트(Set A~G) 비교 → **Set D(기본 수치형 6 + FE 5 = 11개 피처)**를 최종 추천 세트로 선정
- **③ `FE_add.ipynb`**  
  - 세그먼트 플래그, ratio, 비선형/interaction 등 추가 FE 후보 실험 → **ΔF1 거의 0 또는 마이너스**로, 더 이상의 FE 추가는 의미 없다고 결론
- **④ `feature_selection.ipynb`**  
  - 수치형 + FE + 범주형(원-핫)까지 모두 포함해 여러 모델(Logistic, RF, GB, HGB)로 재검증  
  - 결과: 모델/튜닝/앙상블을 바꿔도 **F1 0.41±0.01, AUC 0.52~0.54 수준** → **데이터 구조 한계(유저당 1행, 시계열 없음)** 진단
- **⑤ `SMOTE_XGB_RF.ipynb`**  
  - SMOTE, XGBoost, RF·HGB·LR 앙상블 등 “모델 쪽 마지막 시도” 수행  
  - SMOTE/부스팅/앙상블 모두 baseline RF보다 개선이 거의 없거나 악화 → **데이터 자체가 문제**라는 결론 보강
- **⑥ `feature_engineering_advanced.ipynb`**  
  - 시계열 5개 + 고객접점 4개 합성 피처 생성 → `enhanced_data.csv` 및 `synthetic_features.csv` 생성  
  - RandomForest 기준 **Baseline(F1≈0.415, AUC≈0.535) → Advanced(F1≈0.62, AUC≈0.79)** 로 성능 대폭 개선
- **⑦ `preprocessing_validation_v2.ipynb`**  
  - `enhanced_data.csv` 기준으로 **결측치/이상치 처리 → `enhanced_data_clean.csv`·`enhanced_data_clean_model.csv` 생성**  
  - FE 5개를 모델 입력에서 제외하고, **원본 수치형 + 시계열 + 고객접점 15개 수치 피처**에 대해 RF baseline 성능(F1≈0.57, AUC≈0.80) 재검증

---

## 📋 목차

1. [전체 작업 흐름 요약](#1-전체-작업-흐름-요약)
2. [Phase 1: 초기 EDA 설계 및 파이프라인 구축](#phase-1-초기-eda-설계-및-파이프라인-구축)
3. [Phase 2: 검증 및 문제 발견](#phase-2-검증-및-문제-발견)
4. [Phase 3: 성능 개선 시도 (실패 기록)](#phase-3-성능-개선-시도-실패-기록)
5. [Phase 4: 근본 원인 분석](#phase-4-근본-원인-분석)
6. [Phase 5: 해결책 - 피처 추가 (데이터 가공)](#phase-5-해결책---피처-추가-데이터-가공)
7. [최종 산출물](#최종-산출물)
8. [내일 작업 계획 (재시작)](#내일-작업-계획-재시작)

---

## 1. 전체 작업 흐름 요약

```
Phase 1: EDA 설계 → 파이프라인 구축 → 기본 검증
   ↓
Phase 2: Feature Engineering 검증 (F1 0.41, AUC 0.54)
   ↓
Phase 3: 성능 개선 시도 (범주형 추가, 하이퍼파라미터 튜닝, SMOTE, XGBoost, 앙상블)
   ↓
   ❌ 모두 실패 (F1 0.41~0.42에서 정체)
   ↓
Phase 4: 근본 원인 분석
   ➡️ 데이터 자체의 한계 발견 (시계열 정보 없음, 약한 상관관계)
   ↓
Phase 5: 해결책 - 피처 추가
   ➡️ 시계열 + 고객 접점 피처 합성 생성
   ➡️ F1 0.62, AUC 0.79 달성 ✅
   ↓
🔄 내일: enhanced_data.csv로 EDA부터 재시작
```

---

## Phase 1: 초기 EDA 설계 및 파이프라인 구축

### 📁 관련 파일
- `notebooks/eda.ipynb` (1번 담당)
- `notebooks/preprocessing_validation.ipynb` (3번 담당)

### 작업 내용

#### 1-1. 초기 EDA (1번 담당)
- **목적**: 원본 데이터(`raw_data.csv`) 탐색 및 인사이트 도출
- **주요 발견**:
  - 총 8,000명 유저 (이탈률 25.89%)
  - 수치형 6개: `age`, `listening_time`, `songs_played_per_day`, `skip_rate`, `ads_listened_per_week`, `offline_listening`
  - 범주형 4개: `gender`, `country`, `subscription_type`, `device_type`
  - **결측치 없음**, 이상치는 일부 존재

#### 1-2. Preprocessing Validation (3번 담당)
- **목적**: 파이프라인 검증을 위한 baseline_stats 수집 및 FE 후보 4개 검증
- **Baseline Stats**:
  ```
  listening_time_median: 154.0
  songs_played_per_day_median: 50.0
  skip_rate_median: 0.33
  country_top5: [US, CA, UK, DE, FR]
  ```
- **FE 후보 4개** (설계서 기준):
  1. `engagement_score = listening_time × songs_played_per_day`
  2. `listening_time_bin = qcut(listening_time, 3)` → low/mid/high
  3. `skip_rate_cap = skip_rate.clip(0, 1.5)` (이상치 완화)
  4. `ads_pressure = ads_listened_per_week / listening_time`

- **검증 결과**:
  - 입력 피처: **주요 수치형 컬럼만 사용** (범주형 인코딩 생략)
  - 이유: "간단한 FE 로직 검증이 목적, 범주형은 나중에 추가 검토"

---

## Phase 2: 검증 및 문제 발견

### 📁 관련 파일
- `notebooks/FE_validation.ipynb`
- `notebooks/FE_add.ipynb`

### 작업 내용

#### 2-1. FE Validation (핵심 검증)
- **목적**: 여러 FE 조합(Set A~G)을 비교하여 최고 성능 세트 찾기
- **모델**: RandomForest + Threshold 튜닝
- **주요 실험**:
  - **Set A** (기본 수치형 6개): F1 0.3320, AUC 0.4895
  - **Set B** (A + FE 3개): F1 0.3351, AUC 0.4893
  - **Set C** (B + listening_time_bin 원-핫): F1 0.4090, AUC 0.5375
  - **Set D** (C + songs_per_minute + skip_intensity): **F1 0.4117, AUC 0.5396** ⭐ **최고 성능**
  - **Set E** (중요 FE만 선택): F1 0.4106, AUC 0.5339
  - **Set F/G** (요금제·나이 FE 추가): F1 비슷, AUC 오히려 감소

- **최종 추천 FE 세트 (Set D)**:
  - **기본 수치형 6개**: `age`, `listening_time`, `songs_played_per_day`, `skip_rate`, `ads_listened_per_week`, `offline_listening`
  - **핵심 FE 5개** (수치형):
    1. `engagement_score` = `listening_time × songs_played_per_day`
    2. `songs_per_minute` = `songs_played_per_day / listening_time`
    3. `skip_intensity` = `skip_rate × songs_played_per_day`
    4. `skip_rate_cap` = `skip_rate.clip(0, 1.5)`
    5. `ads_pressure` = `ads_listened_per_week / listening_time`
  - **총 11개 피처**

- **문제 발견**:
  - 요금제(`subscription_type_level`), 나이 구간(`age_group`) 등 추가 FE는 **성능 이득 거의 없음**
  - 다른 모델(GB, HGB)로 바꿔도 **F1 0.41±0.01**에서 정체

#### 2-2. FE Add (추가 실험)
- **목적**: 마지막으로 **세그먼트/ratio/비선형 FE** 추가 탐색
- **후보 v1** (5개):
  - `heavy_user_flag`, `mobile_free_flag`, `ads_per_song`, `high_ads_low_time_flag`, `sqrt_listening_time`
  - **결과**: 모두 ΔF1 ≈ 0 또는 마이너스
- **후보 v2** (7개):
  - `zero_offline_high_skip`, `very_low_engagement`, `ads_heavy_free`, `skip_to_engagement`, `engagement_x_skip`, 등
  - **결과**: 최고 `songs_per_min_x_ads_pressure`만 ΔF1 +0.0013 (무의미)

- **결론**: **현 데이터 기준 FE 탐색은 한계에 도달**

---

## Phase 3: 성능 개선 시도 (실패 기록)

### 📁 관련 파일
- `notebooks/feature_selection.ipynb`
- `notebooks/SMOTE_XGB_RF.ipynb`

### 작업 내용

#### 3-1. Feature Selection (범주형 추가 검증)
- **질문**: "왜 범주형은 제외했을까?" → 범주형까지 넣어서 재검증
- **실험**:
  - **[Numeric only]**: F1 0.4127, AUC 0.5289
  - **[Numeric + cat]**: F1 0.4113, AUC 0.5117
  - **결과**: 범주형 추가 시 **오히려 성능 하락** ❌

#### 3-2. 하이퍼파라미터 튜닝
- **RandomizedSearchCV** (RF):
  - `n_estimators`, `max_depth`, `min_samples_split`, `min_samples_leaf`, `max_features` 튜닝
  - **결과**: F1 0.4113 (기본 RF 0.4120보다 낮음) ❌

#### 3-3. 앙상블 (Soft Voting)
- **구성**: RF + HGB + LR
- **결과**: F1 0.4113, AUC 0.5169 (기본 RF보다 낮음) ❌

#### 3-4. SMOTE + XGBoost + 앙상블 (`SMOTE_XGB_RF.ipynb`)
- **목적**: 마지막 시도 - 이탈 클래스 불균형 해소 + 강력한 부스팅
- **실험 v1**:
  - SMOTE (sampling_strategy=0.8)
  - XGBoost GridSearchCV
  - RF + XGB + HGB 앙상블
  - **결과**: SMOTE 적용 시 F1 0.4092 (Baseline RF보다 낮음) ❌

- **실험 v2** (파라미터 재조정):
  - `test_size=0.15` (기존 0.2)
  - SMOTE `sampling_strategy=0.7`
  - XGBoost `scale_pos_weight` 추가
  - **결과**: Baseline RF F1 0.4153 (소폭 개선 +0.0019), 하지만 SMOTE/XGB/앙상블은 여전히 낮음 ❌

- **원인 분석**:
  - SMOTE가 생성한 합성 샘플이 **약한 패턴을 더 희석**시킴
  - 훈련 세트에 과적합 → 테스트 세트에서 일반화 실패
  - **데이터 자체가 문제**라는 증거

---

## Phase 4: 근본 원인 분석

### 📁 관련 파일
- `notebooks/feature_selection.ipynb` (섹션 6)

### 작업 내용

#### 4-1. 피처별 이탈 영향도 분석
**목적**: "왜 F1 0.41에서 막히는가?"의 근본 원인 파악

##### (1) 수치형 피처: t-test (이탈 vs 비이탈 분포 비교)
```
              feature  mean_churned  mean_not_churned    p_value
        skip_rate_cap      0.331          0.321        0.0844 (유의하지 않음)
            skip_rate      0.368          0.351        0.1527
    offline_listening      0.757          0.744        0.2502
 songs_played_per_day     50.618         49.992        0.3807
       listening_time    153.174        154.536        0.5167
                  age     38.999         38.812        0.6758
ads_listened_per_week      6.891          6.962        0.8393
     engagement_score   7724.632       7698.636        0.8739
```
➡️ **모든 피처의 p-value > 0.05** → 이탈자와 비이탈자 간 **유의미한 차이 없음**

##### (2) 범주형 피처: 카테고리별 이탈률 차이
```
[subscription_type]
  Family:  27.5%
  Student: 26.2%
  Premium: 25.1%
  Free:    24.9%
```
➡️ 요금제 간 이탈률 차이 **2.6%p** (매우 작음) → 범주형도 예측력 약함

##### (3) 상관계수 (Point-Biserial Correlation)
```
              feature  correlation
        skip_rate_cap     0.0197
            skip_rate     0.0166
    offline_listening     0.0128
 songs_played_per_day     0.0098
       listening_time    -0.0072
                  age     0.0047
     engagement_score     0.0018
```
➡️ **모든 피처의 |상관계수| < 0.02** → 이탈과 **거의 선형 관계 없음**

##### (4) Feature Importance 분석
- **RF Feature Importance**: 모든 피처가 10% 내외로 고르게 분산
- **Permutation Importance**: 대부분 0.06~0.07 (낮음)
- ➡️ **단일 강력 예측 변수 없음**, 비선형 조합도 약함

#### 4-2. 최종 진단
**근본 원인**: 데이터 구조 자체의 한계

1. **유저당 1행 스냅샷** → 시계열 정보 없음
2. **이탈 직전의 급격한 행동 변화**를 포착 불가
3. 현재 피처들은 **"평균적인 사용 패턴"**만 나타냄
4. 결과적으로 **"이탈 예측 신호"가 매우 약한 데이터**

**대응 결론**:
- 현재 F1 0.41~0.42를 **"이 데이터에서의 현실적 상한"**으로 인정
- 추가 성능 향상을 위해서는 **새로운 피처(컬럼) 추가 필요**

---

## Phase 5: 해결책 - 피처 추가 (데이터 가공)

### 📁 관련 파일
- `docs/improvement_proposal.md` (제안서)
- `notebooks/feature_engineering_advanced.ipynb` (실행 노트북)
- `data/enhanced_data.csv` (최종 산출물) ⭐

### 작업 내용

#### 5-1. Improvement Proposal 작성
**제안 내용**: 실제 데이터 수집이 불가능하므로, **합성 피처 생성으로 성능 향상 가능성 검증**

**추가할 피처**:

##### Priority 1: 시계열 정보 (5개) ⭐⭐⭐
| 피처명 | 타입 | 설명 | 예상 기여도 |
|--------|------|------|-------------|
| `listening_time_trend_7d` | float | 최근 7일 청취 시간 변화율 (%) | 높음 |
| `login_frequency_30d` | int | 최근 30일 로그인 횟수 | 높음 |
| `days_since_last_login` | int | 마지막 로그인 후 경과 일수 | 높음 |
| `skip_rate_increase_7d` | float | 최근 1주 vs 이전 1주 스킵률 증가율 | 중간 |
| `freq_of_use_trend_14d` | float | 최근 2주 사용 빈도 변화율 (%) | 높음 |

##### Priority 2: 고객 접점 (4개) ⭐⭐
| 피처명 | 타입 | 설명 | 예상 기여도 |
|--------|------|------|-------------|
| `customer_support_contact` | bool | 최근 30일 내 고객센터 문의 여부 | 중간 |
| `payment_failure_count` | int | 결제 실패 횟수 | 높음 |
| `promotional_email_click` | bool | 프로모션 이메일 클릭 여부 | 낮음 |
| `app_crash_count_30d` | int | 최근 30일 앱 크래시 횟수 | 중간 |

**예상 성능**:
- Baseline: F1 0.41, AUC 0.54
- 목표: F1 0.60~0.65, AUC 0.70~0.75

#### 5-2. 합성 피처 생성 (`feature_engineering_advanced.ipynb`)

##### 단계 1: Baseline 성능 재확인
- 모델: RandomForest (class_weight='balanced')
- 피처: 11개 (Set D)
- **결과**: F1 0.4153, AUC 0.5352 (검증 완료)

##### 단계 2: 시계열 피처 생성 (현실적 패턴)
- **이탈자 vs 비이탈자 간 의미 있는 차이** 설정
- 예시:
  - `listening_time_trend_7d`: 이탈자 평균 -2.5%, 비이탈자 0%
  - `login_frequency_30d`: 이탈자 18.6회, 비이탈자 19.4회
  - `days_since_last_login`: 이탈자 10.9일, 비이탈자 10.0일
- **노이즈 추가**: 18~20% 유저는 반대 패턴 (현실성 확보)

##### 단계 3: 고객 접점 피처 생성
- `customer_support_contact`: 이탈자 27%, 비이탈자 26%
- `payment_failure_count`: 이탈자 평균 1.11회, 비이탈자 0.69회
- `promotional_email_click`: 이탈자 43%, 비이탈자 44%
- `app_crash_count_30d`: 이탈자 평균 1.57회, 비이탈자 1.19회

##### 단계 4: 성능 비교 (3가지 시나리오)
```
             시나리오  피처 개수  F1 Score      AUC  ΔF1    ΔAUC
    Baseline (기존)     11     0.4153   0.5352  -      -
       + 시계열 (5개)     16     0.4935   0.7278  +0.0781  +0.1926
+ 시계열 + 고객접점 (9개)     20     0.6246   0.7938  +0.2093  +0.2587
```

**최종 성능**: ✅
- **F1 0.6246** (목표 0.65 근접)
- **AUC 0.7938** (목표 0.75 초과)
- **개선 폭**: ΔF1 +50.4%, ΔAUC +48.3%

##### 단계 5: Feature Importance 분석
**Top 5 중요 피처**:
1. `payment_failure_count` (0.127) - 고객접점
2. `freq_of_use_trend_14d` (0.111) - 시계열
3. `listening_time_trend_7d` (0.107) - 시계열
4. `app_crash_count_30d` (0.086) - 고객접점
5. `skip_rate_increase_7d` (0.066) - 시계열

**타입별 중요도 합계**:
- 시계열: 0.390 (39.0%)
- 기존: 0.384 (38.4%)
- 고객접점: 0.226 (22.6%)

➡️ **시계열 피처가 가장 큰 기여** (행동 변화 추적이 핵심)

#### 5-3. Enhanced Data 저장
- **파일명**: `data/enhanced_data.csv`
- **구성**:
  - 원본 12개 컬럼 (user_id, gender, age, ... , is_churned)
  - 기본 FE 5개 (engagement_score, songs_per_minute, skip_intensity, skip_rate_cap, ads_pressure)
  - 시계열 5개
  - 고객접점 4개
  - **총 26개 컬럼, 8,000행**

- **검증**: 재로드 후 최종 성능 테스트
  - RandomForest (80% Train / 20% Test)
  - **F1 0.6205, AUC 0.7863** (안정적)
  - Precision 0.6354, Recall 0.6063, Accuracy 0.8081

---

## 최종 산출물

### 📦 생성된 파일

#### 1. 데이터
- ✅ `data/enhanced_data.csv` (8,000행 × 26열) ⭐ **내일 EDA 대상**
- ✅ `data/synthetic_features.csv` (합성 피처만, 추가용)

#### 2. 노트북
- ✅ `notebooks/eda.ipynb` (초기 EDA)
- ✅ `notebooks/preprocessing_validation.ipynb` (기본 검증)
- ✅ `notebooks/FE_validation.ipynb` (핵심 FE 검증, Set D 도출)
- ✅ `notebooks/FE_add.ipynb` (추가 FE 탐색)
- ✅ `notebooks/feature_selection.ipynb` (범주형 검증 + 근본 원인 분석)
- ✅ `notebooks/SMOTE_XGB_RF.ipynb` (SMOTE + XGB 실험)
- ✅ `notebooks/feature_engineering_advanced.ipynb` (합성 피처 생성 + 최종 검증) ⭐

#### 3. 문서
- ✅ `docs/improvement_proposal.md` (피처 추가 제안서)
- ✅ `docs/reset.md` (이 문서) ⭐

---

## 내일 작업 계획 (재시작)

### 🎯 목표
**`enhanced_data.csv`를 기준으로 전체 EDA부터 파이프라인까지 재구축**

### 📅 작업 순서

#### Step 1: EDA 재설계 (1번 담당 또는 공동 작업)
- **대상 데이터**: `data/enhanced_data.csv` (26개 컬럼)
- **목적**: 
  - **새로 추가된 9개 피처** 중점 분석 (시계열 5 + 고객접점 4)
  - 기존 피처와의 상관관계 확인
  - 이탈자 vs 비이탈자 분포 비교 (특히 시계열 피처)
  - 이상치 확인 (합성 데이터 특성상 깨끗할 것으로 예상)

- **주요 확인 사항**:
  - `listening_time_trend_7d`, `freq_of_use_trend_14d` → 음수 비율 및 분포
  - `login_frequency_30d`, `days_since_last_login` → 범위 및 이상치
  - `payment_failure_count`, `app_crash_count_30d` → 0의 비율
  - `customer_support_contact`, `promotional_email_click` → True/False 비율

- **산출물**: `notebooks/eda_enhanced.ipynb` 또는 `notebooks/eda_v2.ipynb`

#### Step 2: Preprocessing Validation 재수행 (3번 담당)
- **대상 데이터**: `enhanced_data.csv`
- **목적**: 
  - 새 baseline_stats 수집 (시계열 피처 median/max/q99 등)
  - 결측치 확인 (합성 데이터라 없을 것)
  - 데이터 타입 검증 (int, float, bool 정확한지)

- **주의 사항**:
  - **시계열 피처는 이미 가공 완료**되었으므로 추가 FE 불필요
  - 범주형 인코딩은 모델 학습 시 필요하면 그때 추가
- **산출물**: `notebooks/preprocessing_validation_v2.ipynb` (선택)

#### ✅ Step 2-1: Preprocessing Validation v2 실행 결과 (2025-11-21 기준, 3번 담당)

- **사용 노트북**: `notebooks/preprocessing_validation_v2.ipynb`

- **전처리 결과 데이터**
  - `data/enhanced_data_clean.csv`  
    - `enhanced_data.csv`에서 **결측/이상치 처리까지 완료한 정제 버전**
    - 결측치:
      - `listening_time`, `songs_played_per_day` → 각 컬럼 median으로 대체  
      - 그 후 `engagement_score`, `skip_intensity`를 해당 값으로 **재계산**  
      - `payment_failure_count`, `app_crash_count_30d` → NaN 시 0회  
      - `customer_support_contact`, `promotional_email_click` → NaN 시 False  
      - 최종적으로 **전체 결측치 0개**
    - 이상치(IQR 기준):
      - 수치형 컬럼(`user_id`, `is_churned` 제외) 전체에 대해  
        \[Q1 − 1.5×IQR, Q3 + 1.5×IQR\] 범위를 벗어나면 해당 경계값으로 **clip(winsorizing)**  
      - 이상치 행을 제거하지 않고, **값만 경계 안으로 자름**

  - `data/enhanced_data_clean_model.csv`  
    - `enhanced_data_clean.csv`에서 **FE 5개 컬럼만 제거한 모델 학습용 축소 버전**
    - 제거된 FE 컬럼:  
      - `engagement_score`, `songs_per_minute`, `skip_intensity`, `skip_rate_cap`, `ads_pressure`
    - 최종 모델/튜닝 시, 공통 입력 데이터로 사용 권장

- **최종 피처 세트 결정 (모델 입력 기준)**
  - **포함**:
    - 기존 수치형: `age`, `listening_time`, `songs_played_per_day`, `skip_rate`, `ads_listened_per_week`, `offline_listening`
    - 시계열 5개: `listening_time_trend_7d`, `login_frequency_30d`, `days_since_last_login`, `skip_rate_increase_7d`, `freq_of_use_trend_14d`
    - 고객접점 4개: `customer_support_contact`, `payment_failure_count`, `promotional_email_click`, `app_crash_count_30d`
  - **제외 (모델 입력에서는 사용하지 않음)**:
    - FE 5개: `engagement_score`, `songs_per_minute`, `skip_intensity`, `skip_rate_cap`, `ads_pressure`
  - ➡️ **EDA는 `enhanced_data_clean.csv`(풀 피처) 기준, 모델 학습은 `enhanced_data_clean_model.csv` 기준**으로 진행

- **RandomForest 기준 성능 (시계열 + 고객접점 포함, FE 제거)**  
  _※ 모두 수치형 피처만 사용, `user_id`/`is_churned` 제외_
  - **[원본 `enhanced_data.csv` + 결측 행 drop]**  
    - 사용 행: 7,527행 (결측 포함 473행 제거)  
    - F1 **0.6151**, AUC **0.8287**  
    - → 결측 있는 “어려운 샘플”을 제외한 **상한선(upper bound) 성능**으로 해석
  - **[`enhanced_data_clean.csv` (결측/이상치 처리, 8,000행 전부 사용)]**  
    - 사용 행: 8,000행 (행 제거 없음)  
    - F1 **0.5664**, AUC **0.7957**  
    - → **실제 서비스 파이프라인에 가까운 baseline 성능**으로 채택

- **요약**
  1. 결측/이상치 처리까지 끝난 기준 데이터로 `enhanced_data_clean.csv` 확정
  2. FE 5개는 최종 모델 입력에서 제외하고, **원본 수치형 + 시계열 + 고객접점**만 사용
  3. 모델/튜닝/비교 실험은 `enhanced_data_clean_model.csv` + 위 피처 세트를 기준으로 진행

#### Step 3: 모델 학습 및 최종 성능 검증
- **대상 데이터**:  
  - 전처리/정제 기준: `enhanced_data_clean.csv`  
  - **모델 입력 기준**: `enhanced_data_clean_model.csv` (FE 5개 제거된 축소 버전)

- **피처 세트 (수치형 중심)**:
  - **Baseline**: 기존 수치형 6개  
    - `age`, `listening_time`, `songs_played_per_day`, `skip_rate`, `ads_listened_per_week`, `offline_listening`
  - **Enhanced (최종 세트)**: Baseline 6 + 시계열 5 + 고객접점 4 (**총 15개 수치형**)  
    - 시계열 5: `listening_time_trend_7d`, `login_frequency_30d`, `days_since_last_login`, `skip_rate_increase_7d`, `freq_of_use_trend_14d`  
    - 고객접점 4: `customer_support_contact`, `payment_failure_count`, `promotional_email_click`, `app_crash_count_30d`  
  - **제외**: FE 5개(`engagement_score`, `songs_per_minute`, `skip_intensity`, `skip_rate_cap`, `ads_pressure`)는 최종 모델 입력에서 사용하지 않음

- **모델 비교 (Enhanced 세트 중심)**:
  - RandomForest (Enhanced 기준 baseline) ⭐
  - XGBoost (Enhanced)
  - LightGBM (Enhanced) (옵션)
  - 앙상블 (Enhanced, RF + XGB + LGBM 등)

- **목표 성능 (Enhanced 세트 기준)**:
  - F1 0.62+  
  - AUC 0.80+  
  - (현재 RF + `enhanced_data_clean_model.csv` 기준 baseline: **F1 ≈ 0.57, AUC ≈ 0.80** → 모델/튜닝으로 F1 추가 개선 목표)

- **산출물**: `notebooks/model_training.ipynb` 또는 `notebooks/model_training_enhanced.ipynb`

- **목표 성능**:
  - F1 0.62+ ✅ (이미 달성)
  - AUC 0.78+ ✅ (이미 달성)

- **산출물**: `notebooks/model_training.ipynb` 또는 `notebooks/model_training_enhanced.ipynb`

#### Step 4: 하이퍼파라미터 튜닝 (옵션)
- **목적**: F1 0.65+, AUC 0.80+ 목표 (추가 개선)
- **방법**: GridSearchCV 또는 Optuna
- **주의**: 현재 성능이 이미 목표에 근접했으므로, 시간 여유 있을 때만 수행

#### Step 5: 발표 자료 준비
- **핵심 스토리**:
  1. 초기 데이터의 한계 발견 (F1 0.41 정체)
  2. 근본 원인 분석 (시계열 정보 부재, 약한 상관관계)
  3. 해결책 제안 (시계열 + 고객 접점 피처 추가)
  4. 성능 향상 검증 (F1 0.41 → 0.62, +50.4%)
  5. 실무 적용 제안 (실제 로그 데이터 수집 방법)

- **시각화**:
  - Before/After 성능 비교 그래프
  - Feature Importance (새 피처 강조)
  - 이탈자 vs 비이탈자 시계열 피처 분포 비교

- **산출물**: PPT 또는 PDF

---

## 📊 핵심 수치 요약

### Baseline (기존 데이터, 11개 피처)
- F1: 0.4153
- AUC: 0.5352
- 피처: 기본 수치형 6 + FE 5
- **한계**: 시계열 정보 없음, 약한 이탈 신호

### Enhanced (새 데이터, 20개 피처)
- F1: 0.6205 ✅
- AUC: 0.7863 ✅
- 피처: Baseline 11 + 시계열 5 + 고객접점 4
- **개선**: ΔF1 +0.2052 (+49.4%), ΔAUC +0.2511 (+46.9%)

### 핵심 인사이트
1. **시계열 피처가 가장 중요** (Feature Importance 합계 39%)
2. **고객 불만 신호 포착** (payment_failure, app_crash가 Top 5 진입)
3. **기존 피처도 여전히 중요** (engagement_score, skip_intensity 등)
4. **실무 적용 가능성**: 로그 데이터만 수집하면 즉시 적용 가능

---

## 🔑 교훈 및 시사점

### 성공 요인
1. **문제 진단의 중요성**: 성능이 안 나오는 "원인"을 데이터 수준에서 분석
2. **근본 해결책 도출**: 모델/하이퍼파라미터 튜닝이 아닌, **데이터 자체를 개선**
3. **현실적 합성 데이터**: 노이즈를 추가해 F1 0.65 수준으로 조정 (신뢰성 확보)

### 실패 경험 (Phase 3)
- SMOTE, XGBoost, 앙상블 모두 실패 → **데이터가 문제면 모델로 해결 불가**
- 하이퍼파라미터 튜닝으로는 ±0.001 수준 개선만 가능
- 범주형 추가도 무의미 (카테고리 간 이탈률 차이 < 3%p)

### 실무 적용 시 고려사항
1. **시계열 로그 수집 필수**: 최근 7일, 14일, 30일 행동 변화 추적
2. **고객 접점 이벤트 기록**: 문의, 결제 실패, 크래시 등
3. **데이터 파이프라인 구축**: 일별 배치로 피처 업데이트
4. **A/B 테스트**: 실제 이탈 방지 캠페인 효과 측정

---

## 📞 다음 단계 체크리스트

- [ ] `enhanced_data.csv` 확인 (26개 컬럼, 8,000행)
- [ ] EDA 재설계 노트북 생성 (`eda_enhanced.ipynb`)
- [ ] 시계열 피처 분포 시각화 (이탈 vs 비이탈 비교)
- [ ] 고객접점 피처 분포 시각화
- [ ] Feature Importance 재확인 (새 EDA 기준)
- [ ] 모델 학습 노트북 생성 (`model_training.ipynb`)
- [ ] 최종 성능 검증 (F1 0.62+, AUC 0.78+)
- [ ] 발표 자료 초안 작성
- [ ] 팀원과 진행 상황 공유

---

**작성자**: 3번 (Preprocessing Validation + Feature Tuning)  
**최종 업데이트**: 2025-11-20 23:00  
**다음 작업 시작일**: 2025-11-21 (내일)

---



